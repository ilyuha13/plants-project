# Заметки для Claude Code

> Этот файл содержит важные правила и паттерны проекта для Claude

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА РАБОТЫ

### Workflow и планирование

**ПРАВИЛО #1: Фокус на текущем плане**

Структура работы состоит из двух фаз:

1. **Сессия планирования**
   - Разбираем долгосрочные и краткосрочные задачи
   - Определяем чёткий список задач (например "задачи перед деплоем")
   - Фиксируем приоритеты

2. **Фаза выполнения**
   - Работаем ТОЛЬКО над задачами из текущего плана
   - НЕ добавляем новые задачи в работу
   - НЕ отвлекаемся на "а давайте ещё это сделаем"

**❌ НЕПРАВИЛЬНО:**
```
User: "Реализуй удаление"
Claude: [делает удаление]
Claude: "А ещё я добавил задачи про типографию в TODO"
Claude: "Давайте ещё исправим кнопки?"
```

**✅ ПРАВИЛЬНО:**
```
User: "Реализуй удаление"
Claude: [делает удаление]
Claude: "Готово! Следующая задача из плана: inventoryNumber"
```

**Исключения (когда можно добавить задачу):**
- Пользователь ЯВНО говорит "это нужно сделать прямо сейчас"
- Пользователь ЯВНО говорит "добавь это в план"

**Вопросы и обучение:**
- Если пользователь задаёт вопрос → отвечать подробно, это процесс обучения
- Объяснять технические детали, причины, альтернативы
- НО после ответа возвращаться к текущему плану, не отвлекаться

**Примеры:**
```
✅ ПРАВИЛЬНО:
User: "Почему зациклился запрос?"
Claude: [подробное объяснение React Query, контекста, etc.]
Claude: "Продолжаем работу? Следующая задача: inventoryNumber"

❌ НЕПРАВИЛЬНО:
User: "Почему зациклился запрос?"
Claude: [объяснение]
Claude: "А ещё давайте исправим типографию во всех карточках!"
```

**Если пользователь упоминает проблему вскользь:**
- Записать в заметки для следующей сессии планирования
- НЕ добавлять в текущий план работы
- НЕ предлагать сделать это сейчас

**Почему это важно:**
- Сохраняет фокус на приоритетных задачах
- Избегает scope creep (расползание задач)
- Позволяет завершить план полностью, а не бросать на полпути
- Снижает когнитивную нагрузку

---

## 🏗️ Архитектурные паттерны

### Аутентификация

**✅ ПРАВИЛЬНО: Использовать `useMe()` из контекста**
```tsx
import { useMe } from '../../lib/ctx'

const MyComponent = () => {
  const me = useMe()

  if (me?.role === 'ADMIN') {
    // ...
  }
}
```

**❌ НЕПРАВИЛЬНО: Делать отдельный запрос через tRPC**
```tsx
// НЕ ДЕЛАТЬ ТАК:
const { data: meData } = trpc.getMe.useQuery()
```

**Причина:** Данные пользователя уже загружены в контексте при инициализации приложения. Повторный запрос - это лишняя нагрузка на сервер и дублирование логики.

---

## 📝 Code Style

### TypeScript

- Использовать строгую типизацию
- Предпочитать type вместо interface для простых типов
- Использовать zod схемы для валидации

### React

- Функциональные компоненты
- Hooks в начале компонента
- Деструктуризация props

---

## 📚 Структура проекта

### Backend
```
backend/src/
├── router/
│   ├── [routeName]/
│   │   ├── index.ts       # Роут с логикой
│   │   └── input.ts       # Zod схема валидации
│   └── index.ts           # Главный router
├── prisma/
│   └── schema.prisma      # Модели БД
└── utils/                 # Утилиты
```

### Frontend
```
webapp/src/
├── pages/                 # Страницы приложения
├── components/            # Переиспользуемые компоненты
├── lib/                   # Утилиты, конфиги
│   ├── ctx.tsx           # React Context (useMe и др.)
│   ├── trpc.ts           # tRPC клиент
│   └── routes.ts         # Функции для генерации путей
└── theme/                 # MUI тема
```

---

## 🔧 Инструменты и библиотеки

### Backend
- tRPC - type-safe API
- Prisma - ORM
- Zod - валидация

### Frontend
- React + TypeScript
- MUI (Material-UI) - компоненты
- tRPC клиент
- React Router

---

## 📋 Правила коммитов

```
feat: описание новой фичи
fix: описание исправления
refactor: описание рефакторинга
docs: обновление документации
chore: техническое (зависимости, конфиг)
style: форматирование, стили

🤖 Generated with Claude Code

Co-Authored-By: Claude <noreply@anthropic.com>
```

---

## 🚨 Важные замечания

### Безопасность
- Всегда проверять роль пользователя для админских действий
- Использовать ADMIN role check на backend
- Frontend проверка только для UI (не для безопасности)

### Performance
- Использовать существующие контексты вместо повторных запросов
- Оптимизировать images (aspect-ratio, object-fit)

---

*Этот файл обновляется по мере обнаружения новых паттернов и правил проекта.*
