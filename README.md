# 🌿 Plants Project

Интернет-магазин комнатных растений с элементами социальной сети. Платформа для продажи растений с возможностью комментирования, обсуждений и персонализации для каждого пользователя.

**Статус:** 🚧 В разработке (MVP)  
**Ссылка на проект:** [greenflagplants.ru](https://greenflagplants.ru)

---

## 📋 Содержание

- [О проекте](#о-проекте)
- [Основные возможности](#основные-возможности)
- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Разработка](#разработка)
- [Deployment](#deployment)
- [Архитектура](#архитектура)
- [Roadmap](#roadmap)

---

## О проекте

Plants Project — это современный e-commerce для продажи комнатных растений, который сочетает:

- 🛒 **Полноценный интернет-магазин** с корзиной и оформлением заказов
- 💬 **Элементы социальной сети** (комментарии, обсуждения на страницах родов/видов)
- 🎨 **Персонализацию** (подсветка купленных растений, история заказов)
- 📱 **Мобильный-first подход** (основной трафик с мобильных устройств)

### Философия

> Страница рода/вида = пост в соцсети. Не просто магазин, а сообщество любителей растений.

---

## Основные возможности

### Для покупателей

- ✅ Просмотр каталога с фильтрацией (род, вариегатность, жизненная форма)
- ✅ Добавление растений в корзину с автоматическим бронированием
- ✅ Оформление заказа (с доставкой или самовывозом)
- ✅ История заказов и персонализация (подсветка купленных растений)
- 🚧 Комментарии и отзывы на страницах растений _(v1.0)_
- 🚧 Система лайков и рейтинга _(v1.1)_

### Для администраторов

- ✅ Добавление/редактирование/удаление товаров (растений, экземпляров)
- ✅ Управление справочниками (рода, вариегатность, жизненная форма)
- ✅ Управление заказами (подтверждение, смена статусов)
- ✅ Telegram-уведомления о новых заказах
- 🚧 Модерация комментариев _(v1.0)_
- 🚧 Статистика продаж _(v1.1)_

---

## Технологии

### Backend

- **Runtime:** Node.js + TypeScript
- **Framework:** Express
- **API:** tRPC (type-safe RPC)
- **Database:** PostgreSQL + Prisma ORM
- **Auth:** JWT + Passport.js
- **Validation:** Zod (shared schemas)
- **Images:** Cloudinary (хранение и трансформации)
- **Notifications:** Telegram Bot API

### Frontend

- **Framework:** React 19 + TypeScript
- **Build tool:** Vite
- **UI Library:** Material-UI (MUI)
- **Routing:** React Router v6
- **Data fetching:** TanStack Query (React Query)
- **State:** Zustand + MobX
- **Forms:** Formik + Yup/Zod
- **Styling:** Emotion (CSS-in-JS)

### DevOps

- **Monorepo:** pnpm workspaces
- **Containerization:** Docker + Docker Compose
- **Reverse proxy:** Nginx
- **SSL:** Let's Encrypt (Certbot)
- **Hosting:** VPS (Ubuntu 22.04)
- **CI/CD:** Git hooks (lint-staged, husky)

---

## Структура проекта

```
plants-project/
├── backend/              # Express + tRPC API
│   ├── src/
│   │   ├── index.ts      # Точка входа
│   │   ├── router/       # tRPC роуты (auto-indexed)
│   │   ├── lib/          # Ядро (trpc, prisma, cloudinary, telegram)
│   │   ├── utils/        # Утилиты
│   │   └── prisma/       # Prisma schema + миграции
│   ├── Dockerfile
│   └── package.json
│
├── webapp/               # React SPA
│   ├── src/
│   │   ├── main.tsx      # Точка входа
│   │   ├── App.tsx       # Router setup
│   │   ├── pages/        # Страницы приложения
│   │   ├── components/   # Переиспользуемые компоненты
│   │   ├── hooks/        # Кастомные хуки
│   │   └── lib/          # tRPC client, роуты
│   └── package.json
│
├── shared/               # Общие Zod схемы
│   └── src/zod.ts
│
├── docker-compose.yml    # Multi-service deployment
├── .env                  # Environment variables
├── CLAUDE.md             # Документация для Claude Code
├── REQUIREMENTS.md       # Требования к проекту
└── README.md             # Этот файл
```

---

## Быстрый старт

### Требования

- Node.js >= 18.x
- pnpm >= 8.x
- PostgreSQL >= 14.x (или Docker)
- (Опционально) Docker + Docker Compose

### Установка

1. **Клонируйте репозиторий:**

```bash
git clone <repo-url>
cd plants-project
```

2. **Установите зависимости:**

```bash
pnpm install
```

3. **Настройте переменные окружения:**

Создайте `.env` в корне проекта:

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/plants_db"

# Auth
JWT_SECRET="your-super-secret-jwt-key"
PASSWORD_SALT="your-password-salt"

# Cloudinary
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN="your-bot-token"
TELEGRAM_ADMIN_CHAT_ID="your-chat-id"

# Environment
HOST_ENV="local"  # или "production"
```

Создайте `.env` в `webapp/`:

```env
VITE_BACKEND_TRPC_URL="http://localhost:3000/trpc"
VITE_BACKEND_URL="http://localhost:3000"
CLAUDINARY_CLOUDE_NAME="your-cloud-name"
```

4. **Инициализируйте базу данных:**

```bash
# Создайте базу данных (если используете локальный PostgreSQL)
createdb plants_db

# Примените миграции
pnpm b pmd

# (Опционально) Заполните справочники
pnpm b prisma studio  # Вручную добавьте данные
```

5. **Запустите dev-сервер:**

```bash
# Запустить backend и frontend одновременно
pnpm dev

# Или раздельно:
pnpm b dev  # Backend на http://localhost:3000
pnpm w dev  # Frontend на http://localhost:5173
```

---

## Разработка

### Структура команд

```bash
# Workspace shortcuts
pnpm b <command>    # backend
pnpm w <command>    # webapp
pnpm s <command>    # shared

# Development
pnpm dev            # Запустить все сервисы

# Database
pnpm b pmd          # Prisma migrate dev
pnpm b pgc          # Prisma generate
pnpm b prisma studio # GUI для БД

# Testing
pnpm b test         # Backend tests (Vitest)
pnpm b test:ui      # Vitest UI

# Code quality
pnpm lint           # Lint all workspaces
pnpm prettify       # Format all workspaces
pnpm types          # Type check all workspaces
```

### Добавление нового tRPC роута

1. Создайте директорию в `backend/src/router/myRoute/`
2. Создайте `input.ts` с Zod схемой:

```typescript
import { z } from 'zod'
export const zMyRouteInput = z.object({ ... })
```

3. Создайте `index.ts`:

```typescript
import { trpc } from '../../lib/trpc'
import { zMyRouteInput } from './input'

export const myRouteTrpcRoute = trpc.procedure.input(zMyRouteInput).query(async ({ ctx, input }) => {
  // ctx.prisma - Prisma client
  // ctx.me - Current user (if authenticated)
  return await ctx.prisma.model.findMany()
})
```

4. Добавьте импорт в `backend/src/router/index.ts`

### Работа с миграциями

```bash
# Создать миграцию после изменения schema.prisma
pnpm b pmd

# Применить миграции в Docker
docker compose exec backend sh -c "cd /app/backend && pnpm prisma migrate deploy"

# Push schema без миграции (только dev!)
pnpm b prisma db push
```

---

## Deployment

### Docker (Production)

1. **Подготовьте `.env` файл** на сервере с production настройками

2. **Соберите и запустите контейнеры:**

```bash
docker compose up -d --build
```

3. **Примените миграции:**

```bash
docker compose exec backend sh -c "cd /app/backend && pnpm prisma migrate deploy"
```

4. **Проверьте статус:**

```bash
docker compose ps
docker compose logs -f backend
```

### Обновление production

```bash
git pull
docker compose up -d --build
# Применить миграции если есть изменения в schema
```

### Структура Docker Compose

- **postgres** - PostgreSQL 16 Alpine
- **backend** - Node.js приложение (порт 3000)
- **nginx** - Reverse proxy с SSL (порты 80/443)
- **certbot** - Автоматическое обновление SSL сертификатов

---

## Архитектура

### Backend: tRPC + Prisma

**Паттерн роутов:** Все роуты авто-индексируются в `backend/src/router/index.ts` через `@index` комментарии.

**Структура роута:**

```
router/myRoute/
  ├── index.ts    # Реализация через trpc.procedure()
  └── input.ts    # Zod валидация
```

**Ключевые модели БД:**

- `User` - Пользователи (nick, password hash, role)
- `Genus`, `Variegation`, `LifeForm` - Справочники для фильтрации
- `Plant` - Виды/сорта растений (каталог)
- `PlantInstance` - Конкретные экземпляры на продажу (инвентарь)
- `Cart` / `CartItem` - Корзина покупок
- `Order` / `OrderItem` - Заказы

**Аутентификация:**

- JWT токены в cookies (frontend: `js-cookie`)
- Passport-JWT middleware
- Context: `me` доступен в tRPC procedures для авторизованных запросов

### Frontend: React + TanStack Query

**Паттерн страниц:** Трёхуровневая архитектура компонентов

```
Level 3: Page (контейнер)
  └─ tRPC запросы
  └─ Обработчики событий
  └─ Level 2: Презентационный компонент
      └─ Форматирование данных
      └─ Level 1: Layout
          └─ Чистый UI
```

**Data fetching:** tRPC React Query hooks

```typescript
const { data } = trpc.getPlants.useQuery()
const mutation = trpc.addPlant.useMutation()
```

**Type safety:** Frontend импортирует типы роутера напрямую:

```typescript
import type { TrpcRouter } from '@plants-project/backend/src/router'
```

End-to-end type safety: Database → Backend → Frontend

---

## Roadmap

### ✅ MVP (текущая версия)

- [x] Аутентификация (регистрация, вход)
- [x] Каталог растений с фильтрацией
- [x] Корзина с автоматическим бронированием
- [x] Оформление заказов
- [x] Админ-панель (добавление товаров, управление заказами)
- [x] Справочники (род, вариегатность, жизненная форма)
- [x] Telegram уведомления для админа

### 🚧 v1.0 (ближайшие планы)

- [ ] Комментарии на страницах растений
- [ ] Отзывы на заказы
- [ ] Поиск по названию растений
- [ ] Улучшенные карточки товаров (свайп фото, персонализация)
- [ ] История заказов для пользователей

### 🔮 v1.1 (будущее)

- [ ] Система лайков и рейтинга
- [ ] Статистика продаж для админа
- [ ] Уведомления об устаревших фото экземпляров
- [ ] Оптимизация изображений (WebP/AVIF)
- [ ] PWA (Progressive Web App)

### 🌟 v2.0 (долгосрочное)

- [ ] Сопутствующие товары (кашпо, опоры, грунт)
- [ ] Маркетплейс (платформа для других продавцов)
- [ ] Мобильное приложение (React Native)

---

## Известные проблемы

1. **Typo в env переменной:** `CLAUDINARY_CLOUDE_NAME` вместо `CLOUDINARY_CLOUD_NAME` (в webapp)
2. **Prisma schema location:** Нестандартное расположение в `backend/src/prisma/` (обычно в корне)
3. **Auto-indexing:** Роуты используют `@index` комментарии для авто-генерации - не редактируйте index
   регионы вручную

не правильное отображение ошибок при регистрации
при авторизации перебрасывает на старую дом страницу
нет инвалидации при добавлении референса

---

## Лицензия

Приватный проект. Все права защищены.

---

## Контакты

- **Developer:** @ilacursukov
- **Project URL:** [api.greenflagplants.ru](https://api.greenflagplants.ru)

---

**Happy coding! 🌱**
