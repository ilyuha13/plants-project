# ✅ ЧЕКЛИСТ ПЕРЕД ПЕРВЫМ ЧИСТЫМ КОММИТОМ

## 🎯 Что было исправлено автоматически:

### ✅ Формы унифицированы (AddPlant, SignUp, SignIn)
- [x] **AddPlantPage:**
  - Stack как form (`component="form"`)
  - Добавлен заголовок "Добавить растение"
  - Padding перенесен в Stack
  - spacing={2} применяется корректно

- [x] **SignUpPage:**
  - elevation={3} (было 2)
  - Stack как form
  - Убраны лишние пробелы в заголовке

- [x] **SignInPage:**
  - Вложенный Stack как form (из-за Link снаружи)
  - Убраны лишние пробелы в заголовке
  - Исправлена опечатка: "зарегистрироваться"

### ✅ Именование файлов стандартизовано
- [x] **AddPlantPage:** index.tsx → AddPlantPage.tsx
- [x] **App.tsx:** Обновлены все импорты (убраны .tsx расширения)
- [x] Все компоненты и страницы используют ComponentName.tsx

### ✅ Backend очищен
- [x] **addPlant/index.ts:** Убран лишний `images.map((image) => image)`

### ✅ .gitignore обновлен
- [x] Добавлены паттерны для user-uploaded images (*.png, *.jpg, *.svg, etc)

### ✅ TypeScript типы проходят
```bash
pnpm types  # ✅ Без ошибок
```

### ✅ ESLint проходит
```bash
pnpm lint  # ✅ Без ошибок
```

---

## 🔧 ЧТО ОСТАЛОСЬ ПРОВЕРИТЬ ВРУЧНУЮ:

### 1. **Проверить структуру файлов**
```bash
ls webapp/src/pages/AddPlantPage/
# Ожидается: AddPlantPage.tsx
```
- [x] Файл правильно назван

### 2. **Проверить .gitignore**
```bash
cat .gitignore
# Должны быть паттерны для images
```
- [x] Images добавлены в .gitignore

---

## 🧪 ТЕСТИРОВАНИЕ:

### Запустить проект:
```bash
pnpm dev
```

### Проверить визуально:

#### AddPlantPage (/add-plant)
- [ ] Заголовок "Добавить растение" отображается
- [ ] Все поля формы расположены вертикально с spacing
- [ ] Кнопка "отправить" работает
- [ ] Alert отображается при ошибках

#### SignUpPage (/sign-up)
- [ ] Заголовок "Регистрация" без лишних пробелов
- [ ] Три поля: Nick, Password, Password again
- [ ] Кнопка "создать пользователя" работает
- [ ] Alert отображается при ошибках

#### SignInPage (/sign-in)
- [ ] Заголовок "Авторизация" без лишних пробелов
- [ ] Два поля: Nick, Password
- [ ] Кнопка "войти" работает
- [ ] Ссылка "зарегистрироваться" без опечатки
- [ ] Alert отображается при ошибках

### Проверить функциональность:
- [ ] Форма AddPlant отправляет данные (попробовать добавить растение)
- [ ] Форма SignUp создает пользователя
- [ ] Форма SignIn авторизует пользователя
- [ ] После submit формы очищаются (если resetOnSuccess: true)

---

## 🎨 ФИНАЛЬНАЯ ПРОВЕРКА:

### Запустить линтеры:
```bash
pnpm lint
pnpm prettify
```

### Проверить git status:
```bash
git status
```

Ожидаемые изменения:
```
M .gitignore
M backend/src/router/addPlant/index.ts
M webapp/src/App.tsx
R webapp/src/pages/AddPlantPage/index.tsx -> AddPlantPage/AddPlantPage.tsx
M webapp/src/pages/SignUp/SignUpPage.tsx
M webapp/src/pages/signInPage/SignInPage.tsx
A TODO.md
A PRECOMMIT_CHECKLIST.md
D backend/public/images/* (множество удаленных файлов - теперь в .gitignore)
```

---

## 🚀 ГОТОВО К КОММИТУ?

Если все чекбоксы отмечены, можно делать коммит:

```bash
git add -A
git commit -m "Clean MVP: unify forms, standardize naming, improve project structure

- Refactor: unify form structure using Stack component='form' pattern
- Fix: standardize file naming (ComponentName.tsx everywhere)
- Fix: remove redundant map in addPlant route
- Fix: add user-uploaded images to .gitignore
- Fix: correct all import paths (remove .tsx extensions)
- Style: consistent Paper elevation={3} across all forms
- Docs: add page titles to all forms (AddPlant, SignUp, SignIn)
- Docs: add architecture decisions section to TODO.md"
```

---

## 📝 ПОСЛЕ КОММИТА:

- [ ] Запустить `git log -1` - проверить коммит
- [ ] Создать новую ветку для Фазы 2:
  ```bash
  git checkout -b feature/plant-detail-page
  ```
- [ ] Открыть TODO.md и начать работу над следующей задачей
