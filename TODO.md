# TODO: Plants Project

## 📋 ПЛАНИРОВАНИЕ И АНАЛИЗ (Приоритет: КРИТИЧНЫЙ)

**ВАЖНО:** Прежде чем начинать новые фичи, нужно навести порядок и спланировать дальнейшую работу.

### Стратегическое планирование (СДЕЛАТЬ ПЕРВЫМ)
- [ ] **Составить детальный план развития проекта**
  - [ ] Определить приоритеты фич (что нужно для MVP, что потом)
  - [ ] Оценить сложность каждой фичи
  - [ ] Составить roadmap на 2-3 спринта
  - [ ] Определить какие компоненты можно переиспользовать
  - [ ] Решить архитектурные вопросы (см. раздел ниже)

### Аудит зависимостей
- [ ] **Проверить все подключенные библиотеки на использование**
  - Возможно есть неиспользуемые зависимости
  - Проверить актуальность версий
  - Удалить лишнее для уменьшения bundle size

**Найденные неиспользуемые зависимости:**
- [ ] **SCSS полностью удален из проекта (детальный план):**
  - [x] Все .scss файлы удалены (перешли на MUI sx)

  **Шаг 1: Удалить зависимости**
  ```bash
  cd webapp
  pnpm remove sass sass-embedded include-media stylelint stylelint-config-prettier-scss stylelint-config-standard-scss
  ```

  **Шаг 2: Удалить скрипты из webapp/package.json**
  - [ ] Удалить строку: `"stylelint": "stylelint ..."`
  - [ ] Удалить строку: `"stylecheck": "sass --update ..."`
  - [ ] Обновить prettify - убрать `scss` из списка расширений

  **Шаг 3: Обновить .husky/pre-commit**
  - [ ] Удалить строки 1-2 (deprecated в Husky v10):
    ```bash
    #!/usr/bin/env sh
    . "$(dirname -- "$0")/_/husky.sh"
    ```
  - [ ] Удалить строку: `pnpm w stylecheck`

  **Шаг 4: Обновить webapp/.lintstagedrc.yml**
  - [ ] Строка 4: изменить `'*.{json,yml,scss}'` на `'*.{json,yml}'`
  - [ ] Строки 5-7: полностью удалить секцию `'*.scss':`

  **Шаг 5: Обновить backend/.lintstagedrc.yml**
  - [ ] Строка 4: изменить `'*.{json,yml,scss}'` на `'*.{json,yml}'`

  **Шаг 6: Обновить .husky/commit-msg (Husky v10)**
  - [ ] Удалить строки 1-2 (deprecated):
    ```bash
    #!/bin/sh
    . "$(dirname "$0")/_/husky.sh"
    ```

- [ ] **Проверить другие библиотеки (результаты grep проверки):**
  - ❌ `@dnd-kit/core` + `@dnd-kit/utilities` - НЕ используется (удалить)
  - ❌ `@toolpad/core` - НЕ используется (удалить)
  - ❌ `classnames` - НЕ используется (удалить)
  - ⚠️ `react-image-crop` - используется только в `ImageEditor.tsx`, который сам нигде не используется
    - Решение: если планируется кроппинг - оставить, иначе удалить и ImageEditor
  - ❌ `mobx` + `mobx-react-lite` - НЕ используется (удалить, используем React Context)
  - ❌ `date-fns` - НЕ используется (удалить)
  - ❌ `include-media` - SCSS библиотека, НЕ используется (удалить)
  - ✅ `multer` (backend) - **ОСТАВИТЬ!** Нужна для миграции на FormData (решение принято)

**Итого к удалению:**
```bash
# webapp - точно не используются
pnpm remove @dnd-kit/core @dnd-kit/utilities @toolpad/core classnames mobx mobx-react-lite date-fns include-media

# Если кроппинг НЕ планируется (решить):
pnpm remove react-image-crop
rm -rf webapp/src/components/ImageEditor

# ❌ НЕ УДАЛЯТЬ multer - нужна для FormData миграции!
```

### Технические долги
- См. разделы выше (SCSS cleanup включает Husky deprecation fix)

---

## 🔥 ПЕРЕД ПЕРВЫМ ЧИСТЫМ КОММИТОМ (Критично)

### ✅ ВЫПОЛНЕНО автоматически:

- [x] **Формы унифицированы** - Stack component="form" pattern
- [x] **AddPlantPage/index.tsx** переименован в AddPlantPage.tsx
- [x] **App.tsx импорты** исправлены (убраны .tsx расширения)
- [x] **backend/addPlant** - лишний map удален
- [x] **.gitignore** - добавлены загруженные изображения
- [x] **Именование файлов** - все компоненты и страницы используют ComponentName.tsx

### Осталось проверить вручную:

- [ ] Запустить `pnpm dev` - убедиться что всё работает
- [ ] Визуально проверить формы (AddPlant, SignUp, SignIn)

### ⚠️ АРХИТЕКТУРНЫЕ РЕШЕНИЯ (Обсудить перед Фазой 2)

#### 1. Загрузка файлов: Base64 vs FormData/Multipart

**✅ РЕШЕНИЕ ПРИНЯТО:** Мигрируем на FormData

**Исследование (WebSearch, январь 2025):**

📊 **Производительность:**
- FormData: 1-13ms (файлы 181KB-1352KB)
- Base64: 0-5ms + 5-7ms encoding = **суммарно медленнее**

📊 **Размер передаваемых данных:**
- Base64: **+33-36% overhead** (теоретически)
- Base64: до **+300%** на практике в некоторых случаях
- FormData: **0% overhead** (оригинальный размер)

📊 **Bandwidth для пользователей:**
- Base64: **расходует на 33% больше** трафика
- Критично для мобильных устройств
- Критично для стран с лимитированным интернетом

**Источники:** Stack Overflow, Medium, DEV Community (2024-2025)

---

**Текущий подход (Base64):**
```tsx
// Frontend
const base64 = await fileToBase64(file)  // +5-7ms encoding
fetch('/trpc/addPlant', {
  body: JSON.stringify({ images: [base64] })  // +33% размер
})

// Backend
const buffer = Buffer.from(base64, 'base64')  // декодирование
await sharp(buffer).toFile(...)  // обработка
```

**Новый подход (FormData):**
```tsx
// Frontend - REST endpoint для загрузки
const formData = new FormData()
formData.append('image', file)  // прямая передача File
const { imageUrl } = await fetch('/api/upload', {
  method: 'POST',
  body: formData
})

// Затем используем URL в tRPC
await trpc.addPlant.mutate({
  variety: '...',
  images: [imageUrl]  // уже загруженное изображение
})

// Backend - multer middleware
app.post('/api/upload', upload.single('image'), async (req, res) => {
  await sharp(req.file.path).toFile(...)  // файл уже на диске
  res.json({ imageUrl: '/images/...' })
})
```

**Trade-off анализ:**

✅ **Преимущества FormData:**
- Быстрее (нет encoding/decoding overhead)
- **На 33% меньше трафика** → экономия bandwidth
- Стандартный подход (industry best practice)
- Progress bar для загрузки (UX улучшение)
- Проще код (нет base64 преобразований)

⚠️ **Недостатки FormData:**
- Нужен отдельный REST endpoint (не tRPC)
- Теряется type-safety для `/api/upload`
- Поддержка 2 типов endpoints

**Вердикт:** Недостатки минимальны (type-safety теряется только для upload endpoint), преимущества значительны (производительность + bandwidth).

---

**План миграции:**

- [x] **Исследование завершено** (WebSearch)
- [ ] **Backend:**
  - [ ] Создать `/api/upload` REST endpoint
  - [ ] Настроить multer middleware
  - [ ] Добавить Sharp обработку
  - [ ] Вернуть URL загруженного изображения
- [ ] **Frontend:**
  - [ ] Создать `useFileUpload` хук
  - [ ] Обновить `ImagesInput` для использования FormData
  - [ ] Добавить progress bar (опционально)
  - [ ] Обновить `AddPlantPage` для нового workflow
- [ ] **Cleanup:**
  - [ ] Удалить base64 conversion utils
  - [ ] Обновить документацию
- [ ] **Testing:**
  - [ ] Протестировать с большими файлами (>5MB)
  - [ ] Проверить на мобильных устройствах
  - [ ] Сравнить производительность до/после

**Файлы для изучения:**
- Git история - возможно есть старая реализация
- `backend/src/utils/saveImafeByBase64ToFile.ts` - текущая логика (заменим)

#### 2. Button: MUI Theme vs Custom Component
- [ ] **Переосмыслить подход к Button компоненту**
  - Текущий подход: Кастомная обертка над MuiButton
  - Альтернатива: Использовать MUI Theme для глобальной кастомизации

**Преимущества MUI Theme подхода:**
```tsx
// webapp/src/theme/theme.ts
const theme = createTheme({
  components: {
    MuiButton: {
      defaultProps: {
        variant: 'outlined',
        type: 'button'
      },
      styleOverrides: {
        root: {
          minWidth: 100
        }
      }
    }
  }
})
```
✅ Не создаем лишние обертки
✅ Работает с полным MUI API
✅ Единая точка кастомизации всех компонентов
✅ Меньше кода

❓ **Вопрос:** Как интегрировать loading state из useForm?

#### 3. MUI Grid: Flexbox vs CSS Grid (Обучение)

**Контекст:** MUI Grid v7.3.4 использует **CSS Flexbox**, а не CSS Grid

**📚 ИЗУЧИТЬ:**
- [ ] **Разница между Flexbox и CSS Grid**
  - [ ] Когда использовать Flexbox (1D layouts, fluid грids)
  - [ ] Когда использовать CSS Grid (2D layouts, row spanning, auto-placement)
  - [ ] Почему MUI выбрала Flexbox для своего Grid компонента

**Результаты исследования (WebSearch, январь 2025):**

✅ **Почему MUI использует Flexbox:**
- **Гибкость для responsive дизайна** - удобные breakpoint props (xs, sm, md, lg, xl)
- **Fluid layouts** - ширины в процентах, адаптируются к родителю
- **Простота** - проще для типичных grid layouts (колонки)

⚠️ **Когда CSS Grid лучше:**
- Row spanning - элементы занимают несколько строк
- Auto-placement - автоматическое размещение элементов
- Сложные 2D layouts

**Вывод:** MUI Grid (Flexbox) идеален для responsive колоночных layouts. Для сложных 2D grid можно использовать Box с `display: grid`.

**Источники:** MUI docs, LogRocket, Stack Overflow

#### 4. MCP серверы для улучшения работы

**📄 ИССЛЕДОВАНИЕ ЗАВЕРШЕНО:** См. [docs/MCP_RESEARCH.md](docs/MCP_RESEARCH.md)

- [x] **Провести WebSearch по MCP** (2025-01-24)
- [x] **Изучить available MCP servers** (PostgreSQL, React, RAG)
- [x] **Понять преимущества и use cases**
- [ ] **РЕШИТЬ: Подключать ли MCP сейчас или после MVP?**

**Краткий вердикт исследования:**
- ✅ MCP - мощный инструмент для production
- ✅ Есть MCP для PostgreSQL, React Docs, Codebase RAG
- ⚠️ WebSearch покрывает ~80% текущих нужд
- ⚠️ MCP добавляет complexity setup
- 💡 **Рекомендация:** Оставить для production phase

**Потенциальные MCP для Plants Project:**
- 📊 **PostgreSQL MCP** (index tuning, explain plans) - СРЕДНИЙ приоритет
- 📚 **React Docs MCP** (semantic search) - НИЗКИЙ приоритет
- 🧠 **Claude Context** (RAG для codebase) - ВЫСОКИЙ при росте проекта

#### 5. RAG (Retrieval-Augmented Generation) для AI

**📄 ИССЛЕДОВАНИЕ ЗАВЕРШЕНО:** См. [docs/RAG_RESEARCH.md](docs/RAG_RESEARCH.md)

- [x] **Провести WebSearch по RAG** (2025-01-24)
- [x] **Изучить use cases и best practices 2025**
- [x] **Понять limitations и когда НЕ использовать**
- [x] **RAG vs Fine-Tuning trade-offs**
- [x] **RAG с Claude Code через MCP**
- [ ] **РЕШИТЬ: Нужен ли RAG для Plants Project?**

**Краткий вердикт исследования:**
- ✅ RAG - powerful для dynamic knowledge и codebase understanding
- ✅ Снижает hallucinations, дает citations, real-time info
- ⚠️ Claude Code по умолчанию использует grep (не RAG)
- ⚠️ RAG через MCP (claude-context) экономит 40% tokens
- ❌ **Для Plants Project СЕЙЧАС:** overkill, проект маленький
- 💡 **Рекомендация:** Пересмотреть когда codebase > 200 файлов

**Когда RAG станет полезен:**
- Codebase вырастет значительно
- Добавим external plant databases
- Onboarding новых developers
- Token costs станут проблемой

#### 6. MUI Theme Customization - Рефакторинг стилей

**📄 ИССЛЕДОВАНИЕ ЗАВЕРШЕНО:** См. [docs/MUI_THEMING_RESEARCH.md](docs/MUI_THEMING_RESEARCH.md)

- [x] **Провести WebSearch по MUI theming** (2025-01-24)
- [x] **Изучить sx vs theme vs styled()** (best practices)
- [x] **Найти ОФИЦИАЛЬНЫЙ MUI MCP сервер** ⭐
- [ ] **РЕШИТЬ: Делать ли theme рефакторинг сейчас?**

**Краткий вердикт исследования:**
- ✅ sx prop overuse = bad practice (подтверждено)
- ✅ Theme customization = recommended для consistency
- ✅ Код сейчас перегружен sx - нужен рефакторинг
- ⭐ **НАЙДЕН ОФИЦИАЛЬНЫЙ MUI MCP:** `@mui/mcp`

**Проблема сейчас:**
```tsx
// PlantDetailPage - перегружен sx
<Button sx={{ flex: { xs: 1, sm: 0 } }}>Назад</Button>
<Button sx={{ flex: { xs: 1, sm: 1 } }}>Связаться</Button>
```

**Решение:**
1. Создать `theme/theme.ts` с defaultProps и styleOverrides
2. Убрать большинство sx из компонентов
3. Оставить sx только для truly one-off cases

**План рефакторинга:**
- [ ] Создать `webapp/src/theme/theme.ts`
- [ ] Настроить MuiButton, MuiPaper, MuiStack defaults
- [ ] Обернуть App в ThemeProvider
- [ ] Рефакторить PlantDetailPage
- [ ] Рефакторить AddPlantPage
- [ ] Рефакторить PlantCard
- [ ] Cleanup и verification

**Приоритет:** ВЫСОКИЙ (но после основных фич)
**Оценка:** 2-3 часа
**Польза:** Consistency, cleaner code, easier maintenance

**MUI MCP сервер:**
- ⭐ Установка: `claude mcp add mui-mcp -- npx -y @mui/mcp@latest`
- 📚 Доступ к official MUI docs (no hallucinations!)
- 🎯 50+ components, customization guides
- 💡 **РЕКОМЕНДУЕТСЯ установить!** (особенно для theme работы)

### Финальная проверка
- [ ] Запустить `pnpm types` - должно пройти без ошибок
- [ ] Запустить `pnpm lint` - должно пройти без ошибок
- [ ] Проверить, что приложение запускается (`pnpm dev`)

### Git commit
```bash
git add -A
git commit -m "Clean MVP: unify forms, standardize naming, improve project structure

- Refactor: unify form structure using Stack component='form' pattern
- Fix: standardize file naming (ComponentName.tsx everywhere)
- Fix: remove redundant map in addPlant route
- Fix: add user-uploaded images to .gitignore
- Fix: correct all import paths (remove .tsx extensions)
- Style: consistent Paper elevation={3} across all forms
- Docs: add page titles to all forms (AddPlant, SignUp, SignIn)
- Docs: add architecture decisions section to TODO.md"
```

---

## 🚀 ФАЗА 2: Базовая функциональность (После первого коммита)

### UX улучшения форм (Приоритет: ВЫСОКИЙ)
- [x] **Проблема с полем цены (price):** ИСПРАВЛЕНО (2025-01-24)
  - Было: дефолтное значение 0 требует удаления перед вводом
  - Решение: изменен initialValue с `0` на `''` (пустая строка)
  - Файл: `webapp/src/pages/AddPlantPage/AddPlantPage.tsx:23`

### PlantDetailPage (Приоритет: ВЫСОКИЙ)
- [x] Создать страницу `webapp/src/pages/PlantDetailPage/`
- [x] Добавить роут `getPlantDetailRoute({ plantId: string })`
- [x] Галерея изображений (показать все фото)
- [x] Полное описание растения
- [x] Кнопка "Связаться" (Telegram link)
- [x] Кнопка "Назад к списку"

#### Переделка PlantDetailPage (ПЕРЕД ДЕПЛОЕМ)
- [ ] **Изменить порядок информации:**
  - [ ] Поменять местами сорт (variety) и вид (genus)
    - Сейчас: Сорт (h4) → Вид (h5)
    - Нужно: Вид → Сорт
  - [ ] Убрать огромную цену на зеленом фоне
  - [ ] Переместить цену вниз (рядом с кнопкой "Связаться")

- [ ] **Новый layout для блока действий (внизу):**

  **Вариант A: Цена + две кнопки в Stack**
  ```tsx
  <Stack spacing={2}>
    <Typography variant="h4" align="center">
      {plant.price} ₽
    </Typography>
    <Stack direction="row" spacing={2}>
      <Button onClick={() => navigate(-1)}>Назад</Button>
      <Button href={telegramLink}>Связаться в Telegram</Button>
    </Stack>
  </Stack>
  ```

  **Вариант B: Цена встроена в кнопку**
  ```tsx
  <Stack direction="row" spacing={2}>
    <Button onClick={() => navigate(-1)}>Назад</Button>
    <Button href={telegramLink}>
      Связаться • {plant.price} ₽
    </Button>
  </Stack>
  ```

  **Вариант C: Цена и кнопка в одном Paper**
  ```tsx
  <Paper sx={{ p: 2, display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
    <Typography variant="h5">{plant.price} ₽</Typography>
    <Button href={telegramLink}>Связаться в Telegram</Button>
  </Paper>
  <Button onClick={() => navigate(-1)} fullWidth>Назад к каталогу</Button>
  ```

- [ ] **Улучшить стили:**
  - [ ] Убрать зеленый фон у цены
  - [ ] Сделать информацию более читабельной
  - [ ] Проверить responsive на мобильных

### PlantCard onClick (Приоритет: ВЫСОКИЙ)
- [x] Реализовать переход на PlantDetailPage при клике
- [x] Использовать `useNavigate()` или `<Link>`

### Telegram бот для связи с клиентами (Приоритет: ВЫСОКИЙ)
- [ ] **Создать Telegram бота для приема заявок**
  - [ ] Зарегистрировать бота через @BotFather
  - [ ] Получить bot token
  - [ ] Создать базовую логику приема сообщений
  - [ ] Настроить автоответы (приветствие, подтверждение получения заявки)
  - [ ] Добавить username бота в переменные окружения
  - [ ] Обновить PlantDetailPage с актуальным username

**Текущий статус:**
- ✅ Кнопка "Связаться в Telegram" уже реализована в PlantDetailPage
- ✅ Сообщение автоматически заполняется (сорт, вид, цена)
- ⏳ Placeholder username: `your_bot_username`
- 📝 Нужно создать бота и заменить placeholder

**Формат ссылки:**
```tsx
const telegramLink = `https://t.me/${telegramUsername}?text=${encodeURIComponent(message)}`
```

**Альтернатива (бот с start параметром):**
```tsx
// Если нужна более сложная логика (tracking, analytics)
const telegramLink = `https://t.me/${botUsername}?start=plant_${plantId}`
```

### Превью фото на AddPlantPage (ПЕРЕД ДЕПЛОЕМ)
- [ ] **Добавить галерею превью выбранных фото**
  - [ ] Показывать выбранные фото справа или слева от формы
  - [ ] Layout: Grid с формой слева и галереей справа (или наоборот)
  - [ ] Миниатюры как на PlantDetailPage (ImageList)
  - [ ] Возможность удалить отдельное фото (опционально)

**Layout варианты:**

**Вариант A: Форма слева, превью справа**
```tsx
<Grid container spacing={4}>
  <Grid size={{ xs: 12, md: 6 }}>
    <Paper>
      <Stack component="form" ...>
        {/* Форма */}
      </Stack>
    </Paper>
  </Grid>
  <Grid size={{ xs: 12, md: 6 }}>
    <Paper>
      <Typography variant="h6">Выбранные фото ({images.length})</Typography>
      <ImageList cols={2}>
        {images.map((img, i) => (
          <ImageListItem key={i}>
            <img src={URL.createObjectURL(img)} />
          </ImageListItem>
        ))}
      </ImageList>
    </Paper>
  </Grid>
</Grid>
```

**Вариант B: Форма сверху, превью снизу (мобильные)**
```tsx
<Stack spacing={3}>
  <Paper>{/* Форма */}</Paper>
  {images.length > 0 && (
    <Paper>
      <ImageList cols={3}>{/* Превью */}</ImageList>
    </Paper>
  )}
</Stack>
```

### Header навигация (Приоритет: СРЕДНИЙ)
- [ ] Кнопка "Каталог" → главная страница
- [ ] Сделать Logo кликабельным (переход на главную)
- [ ] Базовый поиск (опционально)
- [ ] **TODO из кода:** Position sticky + opacity для Header (Header.tsx:13)

### TODO из кода (обнаружены при аудите)
- [ ] **AddPlantPage:** Исправить price default value (0 → пустая строка)
  - Файл: `webapp/src/pages/AddPlantPage/AddPlantPage.tsx:24`
  - Проблема: Нужно удалять 0 перед вводом
  - Решение: Либо price: '', либо placeholder без default value

- [ ] **SignIn/SignUp:** Ограничить инпуты (maxLength/валидация)
  - Файлы: `SignInPage.tsx:35`, `SignUpPage.tsx:49`
  - Добавить maxLength для nick, password

- [ ] **PlantDetailPage:** Переместить telegramUsername в .env
  - Файл: `PlantDetailPage.tsx:53`
  - Сейчас: hardcoded 'your_bot_username'
  - Нужно: `VITE_TELEGRAM_BOT_USERNAME` в .env

- [ ] **Galery:** Fullscreen preview при клике на изображение
  - Файл: `Galery.tsx:26`
  - Использовать Dialog или модальное окно
  - Добавить навигацию между фото (prev/next)

### Улучшения PlantCard (Приоритет: СРЕДНИЙ)
- [ ] Форматирование цены: `{plant.price} ₽`
- [ ] Placeholder для отсутствующих изображений
- [ ] Hover эффекты
- [ ] Лучшая типографика

---

## 🎨 РЕФАКТОРИНГ СТИЛЕЙ И КОМПОНЕНТОВ (Отложено, делать поэтапно)

**КОНТЕКСТ:** Фаза 1 завершена (theme расширена, избыточные пропсы убраны). Дальнейший рефакторинг отложен до после деплоя.

**ПРОБЛЕМЫ СЕЙЧАС:**
1. ❌ Кастомный Button компонент игнорирует theme (жестко указан variant="outlined")
2. ⚠️ Box используется для контента (нужен Paper)
3. ⚠️ Повторяющиеся sx пропы в компонентах
4. ⚠️ Дублирование структуры форм (`<Paper><Stack component="form">`)

---

### 📋 СТАДИЯ 1: Минимальная оптимизация (30-60 мин)

**Цель:** Добавить несколько дефолтов в тему, убрать очевидные дублирования

**Задачи:**
- [ ] **Расширить theme.ts:**
  - [ ] `MuiGrid.defaultProps.spacing: 2` → убрать из PlantsListPage:19
  - [ ] `MuiCard.defaultProps.elevation: 2` → для PlantCard

- [ ] **Создать переиспользуемый FormContainer:**
  ```tsx
  // webapp/src/components/FormContainer/FormContainer.tsx
  <FormContainer onSubmit={formik.handleSubmit}>
    <Typography variant="h2">Заголовок</Typography>
    <TextInput ... />
    <Button>отправить</Button>
  </FormContainer>
  ```
  - [ ] Использовать в SignInPage, SignUpPage, AddPlantPage
  - [ ] Убрать дублирование `<Paper><Stack component="form">`

**Результат:** -20-30 строк кода, меньше дублирования

---

### 📋 СТАДИЯ 2: Утилитарные компоненты (1.5-2 часа)

**Цель:** Создать переиспользуемые styled-компоненты для частых паттернов

**Задачи:**
- [ ] **Создать `webapp/src/components/utils/`:**
  - [ ] **CenteredBox** - для loading/error состояний
    ```tsx
    // Используется 3 раза в PlantDetailPage
    <CenteredBox minHeight="80vh">
      <Typography>Загрузка...</Typography>
    </CenteredBox>
    ```
  - [ ] **PageContainer** - обертка для страниц
    ```tsx
    // maxWidth, padding, margin auto
    <PageContainer>...</PageContainer>
    ```
  - [ ] **PriceCard** - стилизованный блок для цены
    ```tsx
    <PriceCard>{plant.price} ₽</PriceCard>
    ```
  - [ ] **ResponsiveImage** - изображение с hover эффектом
    ```tsx
    <ResponsiveImage src={url} alt="..." />
    ```

- [ ] **Рефакторинг компонентов:**
  - [ ] PlantDetailPage: ~210 строк → ~150-160 строк (-25%)
  - [ ] Применить утилиты в других страницах

**Результат:** Меньше sx пропов, больше семантических компонентов, переиспользование

---

### 📋 СТАДИЯ 3: Domain-компоненты (2-3 часа)

**Цель:** Инкапсулировать domain-логику в специализированные компоненты

**Задачи:**
- [ ] **Расширить theme.ts:**
  - [ ] Custom breakpoints для галереи
  - [ ] ImageList styleOverrides
  - [ ] Custom palette tokens (`palette.plant.*`)
  - [ ] Transitions в theme

- [ ] **Создать domain компоненты:**
  - [ ] **PlantImageGallery** - вся логика галереи
    ```tsx
    // Заменит ~70 строк в PlantDetailPage
    <PlantImageGallery images={imageUrls} plantName={plant.variety} />
    ```
  - [ ] **PlantInfoCard** - карточка с информацией
    ```tsx
    // Заменит ~40 строк
    <PlantInfoCard plant={plant} />
    ```
  - [ ] **ContactButton** - кнопка связи с Telegram
    ```tsx
    // С логикой формирования ссылки
    <ContactButton plant={plant} />
    ```

- [ ] **Рефакторинг других страниц:**
  - [ ] PlantsListPage - применить утилиты
  - [ ] AddPlantPage - применить FormContainer и PageContainer
  - [ ] Единообразие во всех страницах

**Результат:** PlantDetailPage: ~210 строк → ~80-90 строк (-50%), полная инкапсуляция логики

---

### 📋 СТАДИЯ 4: Исправить кастомный Button (1 час)

**ПРОБЛЕМА:**
```tsx
// webapp/src/components/Button/Button.tsx:12
<MuiButton sx={{ width: 100 }} variant="outlined">
  // ← Игнорирует theme defaultProps (variant: 'contained')
```

**Варианты решения:**

**Вариант A: Удалить кастомный Button, использовать тему**
```tsx
// theme.ts
MuiButton: {
  defaultProps: {
    variant: 'outlined',
    type: 'button'
  },
  styleOverrides: {
    root: { minWidth: 100 }
  }
}

// Везде использовать:
import { Button } from '@mui/material'
```
✅ Не создаем лишние обертки
✅ Работает с полным MUI API
❓ Как интегрировать loading state из useForm?

**Вариант B: Пробросить пропсы в кастомный Button**
```tsx
export const Button = ({ children, loading, ...props }: ButtonProps) => {
  return (
    <MuiButton disabled={loading} {...props}>
      {loading ? 'подождем...' : children}
    </MuiButton>
  )
}
```
✅ Сохраняем loading logic
⚠️ Нужно типизировать props (MuiButtonProps)

**Задачи:**
- [ ] Выбрать вариант решения
- [ ] Если A: рефакторить useForm + theme
- [ ] Если B: типизировать Button пропсы
- [ ] Обновить все использования Button
- [ ] Убрать sx={{ width: 100 }} если не нужен

**Результат:** Button использует theme, единообразие стилей

---

### 📋 СТАДИЯ 5: Обертка для страниц с query логикой (1-2 часа)

**⚠️ ОТЛОЖЕНО:** Сначала нужно провести исследование best practices для loading состояний

**ПРОБЛЕМА:** Каждая страница дублирует loading/error проверки:
```tsx
// PlantDetailPage, PlantsListPage, и т.д.
if (isLoading) return <span>loading...</span>
if (isError) return <span>error: {error.message}</span>
```

**РЕШЕНИЕ: PageWrapper компонент**
```tsx
// webapp/src/components/PageWrapper/PageWrapper.tsx
export const PageWrapper = ({
  query,
  children
}: {
  query: { isLoading, isError, error, data }
  children: (data) => ReactNode
}) => {
  if (query.isLoading) {
    return <CenteredBox><CircularProgress /></CenteredBox>
  }
  if (query.isError) {
    return <CenteredBox><Alert severity="error">{query.error.message}</Alert></CenteredBox>
  }
  return <>{children(query.data)}</>
}

// Использование:
<PageWrapper query={trpc.getPlant.useQuery(...)}>
  {(data) => (
    <PlantDetailContent plant={data.plant} />
  )}
</PageWrapper>
```

**Задачи:**
- [ ] Создать PageWrapper компонент
- [ ] Добавить красивые loading/error состояния
- [ ] Рефакторить PlantDetailPage
- [ ] Рефакторить PlantsListPage
- [ ] (Опционально) Добавить Skeleton вместо loading

**Результат:** Централизованная логика, красивые loading/error, меньше дублирования

---

### 📊 Итоговый результат всех стадий:

**До рефакторинга:**
- PlantDetailPage: 210+ строк с дублированием
- SignInPage/SignUpPage/AddPlantPage: дублирование форм
- Кастомный Button игнорирует theme
- Loading/error дублируется везде

**После рефакторинга:**
- PlantDetailPage: ~80-90 строк (-50%)
- FormContainer переиспользуется
- Button использует theme
- PageWrapper убирает дублирование
- Легко поддерживать и масштабировать

**Оценка времени:** 6-9 часов суммарно (можно делать поэтапно)

---

## 🛠️ ФАЗА 3: Админ-функционал (Потом)

### EditPlantPage
- [ ] Страница редактирования растения
- [ ] Pre-fill формы существующими данными
- [ ] Показать превью существующих фото
- [ ] Возможность добавить/удалить фото

### DeletePlant
- [ ] Модальное окно подтверждения удаления
- [ ] Backend роут `deletePlant`
- [ ] Защита от случайного удаления

### ImageEditor с кропингом
- [ ] Доделать `ImagesInputWithCrop` компонент
- [ ] Интеграция react-image-crop
- [ ] Preview после кропа

### Админ-панель
- [ ] Отдельная страница `/admin`
- [ ] Список всех растений с кнопками Edit/Delete
- [ ] Статистика (количество растений, последние добавленные)

---

## 🎨 ФАЗА 4: UX и Production (В конце)

### Фильтрация и сортировка
- [ ] Фильтр по роду (genus)
- [ ] Сортировка по цене (возрастание/убывание)
- [ ] Сортировка по дате добавления

### Адаптивность
- [ ] Мобильная версия (< 600px)
- [ ] Планшеты (600px - 960px)
- [ ] Тестирование на реальных устройствах

### Деплой
- [ ] Backend на Railway/Render
- [ ] Frontend на Vercel
- [ ] PostgreSQL на Supabase/Neon
- [ ] Настроить environment variables
- [ ] Настроить CORS для production

---

## 📌 BACKLOG (Низкий приоритет)

### Mega Menu в Header
- [ ] Категории растений (род, жизненная форма, вариегатность)
- [ ] Анимации открытия/закрытия
- [ ] Адаптивная версия (Drawer на мобильных)

### Улучшения безопасности
- [ ] Rate limiting для API
- [ ] CSRF protection
- [ ] Input sanitization

### SEO
- [ ] Meta tags для страниц
- [ ] Open Graph для соц. сетей
- [ ] Sitemap.xml
- [ ] robots.txt

---

## ✅ ВЫПОЛНЕНО

- [x] Упростить модель данных (убрать Species/Variety)
- [x] Создать базовые страницы (AddPlant, PlantsList, Auth)
- [x] Настроить tRPC с типобезопасностью
- [x] Создать базовые компоненты (Button, TextInput, ImagesInput)
- [x] Удалить SCSS, перейти на MUI полностью
- [x] Очистить закомментированный код
- [x] Создать CLAUDE.md документацию

---

## 💡 ИДЕИ ДЛЯ ДРУГИХ ПРОЕКТОВ (не для plants-project)

**Отложить до завершения текущего проекта!**

### Система персонального управления
- [x] Глобальный TODO создан → `~/.claude/TODO.md`
- [ ] Система ведения заметок (идеи по проекту A во время работы над B)
- [ ] Планирование и тайм-менеджмент
- [ ] Документирование процессов
- [ ] Интеграция с Claude Code

**Приоритет:** НИЗКИЙ (после устройства на работу)

**См. также:** `~/.claude/TODO.md` - глобальные задачи и идеи


### Интеграции и синхронизация
- [ ] **Синхронизация каталога с внешними площадками**
  - Проблема: Сейчас синхронизация вручную занимает много времени
  - Площадки: Соцсети (Instagram, VK), торговые площадки (Avito, Юла, и т.д.)
  - Цель: Один раз добавить растение → автоматически появляется везде
  - Есть идеи по реализации
  - **Критично для масштабирования бизнеса**

**Приоритет:** ВЫСОКИЙ (но после MVP)

